behavior eval;

open class Expr {
  def Integer eval() {
    result := 0;
  }
}

open class Sum {
  def Integer eval() {
    result := self.lhs.eval() + self.rhs.eval();
  }
}

open class IntLit {
  def Integer eval() {
    self.value;
  }
}